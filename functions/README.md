This repository deploys Firebase Cloud Functions from `backend`.

The `functions` folder is kept as a marker for CI/CD and project structure expectations.
Source of truth for Cloud Functions code is `backend/src/index.ts`.
